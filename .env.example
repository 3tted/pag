# Copy to apps/web/.env for local development. In Render, set these in the
# service's Environment tab (the blueprint asks for them on first deploy).
# Supabase dashboard → Project Settings → API.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
